services:
  db:
    image: pgvector/pgvector:pg18
    container_name: "pgvector-db"
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
